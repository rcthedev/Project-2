'use strict';function _classCallCheck(instance, Constructor) {if (!(instance instanceof Constructor)) {throw new TypeError('Cannot call a class as a function');}}var Migrate = 
function Migrate(database) {_classCallCheck(this, Migrate);

  this.table = database.define({ 
    name: 'migration', 
    columns: [{ 
      name: 'name', 
      dataType: 'text', 
      notNull: true }, 
    { 
      name: 'appliedAt', 
      dataType: 'timestamp' }] });};