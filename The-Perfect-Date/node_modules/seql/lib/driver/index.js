'use strict';Object.defineProperty(exports, '__esModule', { value: true });exports.getDriver = getDriver;var _drivers = { 
  sqlite: './sqlite' };


function getDriver(dialect) {

  if (dialect in _drivers) {
    var driver = require(_drivers[dialect]);
    driver = driver['default'] || driver;
    driver = new driver();
    return driver;} else 
  {
    throw new Error('Driver ' + dialect + ' not supported');}}